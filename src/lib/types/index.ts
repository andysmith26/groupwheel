/**
 * Utility types for the Friend Hat application.
 *
 * IMPORTANT: This module contains ONLY generic utility types, not domain entities.
 * For domain types (Student, Group, Preference, etc.), import from '$lib/domain'.
 *
 * Migration note (Dec 2025): Domain types were previously defined here but have
 * been consolidated into src/lib/domain/ per architecture guidelines.
 *
 * @module types
 */

// Generic result type for use case returns
export * from './result';
