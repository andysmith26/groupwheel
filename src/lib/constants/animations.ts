/**
 * Animation duration constants
 * 
 * These constants should be used in both CSS (via CSS variables) and JavaScript
 * to ensure animation timings stay in sync.
 */

/**
 * Duration of the success flash animation in milliseconds.
 * Used when a student is successfully dropped into a group or sidebar.
 */
export const FLASH_ANIMATION_DURATION_MS = 700;
